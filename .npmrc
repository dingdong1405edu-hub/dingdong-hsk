# React 19 + next-auth beta have peer-dep conflicts; install must tolerate them.
legacy-peer-deps=true
# Build needs devDependencies (typescript, tailwindcss, postcss, tsx). Railway
# builders run install with NODE_ENV=production (which would omit dev deps), so
# force them in regardless.
include=dev
