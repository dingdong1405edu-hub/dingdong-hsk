[build]
builder = "NIXPACKS"
buildCommand = "npx prisma generate && npm run build"

[deploy]
startCommand = "npx prisma db push --accept-data-loss && npm run db:seed && npm start"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
